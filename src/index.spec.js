import { add } from '.'

describe('String calculator function', function () {
  describe('Step 1: the simplest thing', function () {
    it.todo('accept an empty string')

    it.todo('accept a string with one number')

    it.todo('accept a string with two numbers')
  })
})
